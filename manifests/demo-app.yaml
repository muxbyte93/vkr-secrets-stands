apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-env
  namespace: secrets-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-env
  template:
    metadata:
      labels:
        app: demo-env
    spec:
      containers:
        - name: app
          image: busybox:1.36
          command: ["/bin/sh", "-c"]
          args:
            - while true; do printf 'env='; printf '%s' "$DB_PASSWORD" | sha256sum | cut -c1-8; sleep 10; done
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets-k8s
                  key: db-password
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-file
  namespace: secrets-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-file
  template:
    metadata:
      labels:
        app: demo-file
    spec:
      containers:
        - name: app
          image: busybox:1.36
          command: ["/bin/sh", "-c"]
          args:
            - while true; do printf 'file='; sha256sum /etc/secrets/db-password | cut -c1-8; sleep 10; done
          volumeMounts:
            - name: app-secret
              mountPath: /etc/secrets
              readOnly: true
      volumes:
        - name: app-secret
          secret:
            secretName: app-secrets-k8s
